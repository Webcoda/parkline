import React from 'react';

const ArticleListing = ({ item }) => {
	// const { backgroundColor, itemsPerPage } = item.customFields;
	return (
		<div>Article Listing</div>
	);
}

export default ArticleListing;
